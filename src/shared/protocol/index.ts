// Barrel for the host <-> webview message contract. Import from
// `src/shared/protocol` on both sides; never reach into the files directly.
export * from './messages';
export * from './monitoring';

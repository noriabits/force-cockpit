# Force Cockpit default configuration.
# Override these values in force-cockpit/config.yaml in your workspace.

# Salesforce API version used for all API calls
apiVersion: "65.0"

# Sandbox org names that require confirmation before destructive actions
protectedSandboxes: []

